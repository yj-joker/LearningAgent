# MySQL
MYSQL_HOST=
MYSQL_PORT=
MYSQL_DATABASE=
MYSQL_USER=
MYSQL_PASSWORD=
MYSQL_ROOT_PASSWORD=

#JWT
# 使用 32 字节以上的随机数据生成 Base64 字符串，不要填写普通短密码。
JWT_SECRET=
JWT_EXPIRATION_MILLIS=

# Snowflake（0~1023，多实例部署时每个实例必须不同）
SNOWFLAKE_WORKER_ID=0
