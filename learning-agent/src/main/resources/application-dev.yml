server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/learning_agent?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: mtdssq541yf
    driver-class-name: com.mysql.cj.jdbc.Driver
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    enabled: true

logging:
  level:
    root: info
    com.yjjoker.learningagent: debug
